{
  "name": "hyperliquid-contracts",
  "version": "1.0.0",
  "description": "Smart contracts for HyperEVM TOKEN",
  "scripts": {
    "compile": "npx hardhat compile",
    "test": "npx hardhat test",
    "deploy": "npx hardhat run scripts/deploy.js --network hyperevm-testnet",
    "node": "npx hardhat node",
    "verify": "npx hardhat verify --network hyperevm-testnet"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^4.0.0",
    "@openzeppelin/contracts": "^5.0.0",
    "hardhat": "^2.19.0",
    "dotenv": "^16.3.1"
  }
}